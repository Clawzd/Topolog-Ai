/**
 * SVG paint strings aligned with `--*` tokens in `index.css`
 * (dark default + `html.light`). Import from topology views / minimap.
 */
export const TC = {
  bg: 'hsl(var(--background))',
  workspace: 'hsl(var(--muted) / 0.32)',
  gridMinor: 'hsl(var(--border) / 0.5)',
  gridMajor: 'hsl(var(--border) / 0.72)',
  card: 'hsl(var(--card))',
  secondary: 'hsl(var(--secondary))',
  border: 'hsl(var(--border))',
  borderSoft: 'hsl(var(--border) / 0.55)',
  borderMuted: 'hsl(var(--border) / 0.35)',
  fg: 'hsl(var(--foreground))',
  mutedFg: 'hsl(var(--muted-foreground))',
  mutedFgSoft: 'hsl(var(--muted-foreground) / 0.82)',
  primary: 'hsl(var(--primary))',
  primary12: 'hsl(var(--primary) / 0.12)',
  primary07: 'hsl(var(--primary) / 0.07)',
  primary06: 'hsl(var(--primary) / 0.06)',
  primary05: 'hsl(var(--primary) / 0.05)',
  primary03: 'hsl(var(--primary) / 0.3)',
  primaryStr40: 'hsl(var(--primary) / 0.4)',
  primaryStr50: 'hsl(var(--primary) / 0.5)',
  primaryStr60: 'hsl(var(--primary) / 0.6)',
  primaryStr65: 'hsl(var(--primary) / 0.65)',
  primaryStr70: 'hsl(var(--primary) / 0.7)',
  primaryStr90: 'hsl(var(--primary) / 0.9)',
  popover75: 'hsl(var(--popover) / 0.78)',
  popover92: 'hsl(var(--popover) / 0.92)',
  popover90: 'hsl(var(--popover) / 0.9)',
  ringLine: 'hsl(var(--ring))',
};
